<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>404 Not Found | SWOOSH Bot</title>
  
  <!-- Favicon -->
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
  
  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Poppins:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  
  <!-- Admin Theme CSS -->
  <link rel="stylesheet" href="/css/admin.css">
  <link rel="stylesheet" href="/css/enhanced/admin-theme.css">
  <link rel="stylesheet" href="/css/enhanced/404-fix.css">
  
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--background);
      background-image: 
        radial-gradient(circle at 10% 20%, rgba(102, 51, 153, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 90% 80%, rgba(0, 229, 255, 0.08) 0%, transparent 50%);
      background-attachment: fixed;
      color: var(--text-primary);
      line-height: 1.6;
      min-height: 100vh;
      margin: 0;
      padding: 0;
    }
    
    :root {
      /* Base colors */
      --primary-color: #9c4dff;
      --secondary-color: #00e5ff;
      --accent-color: #00e5ff;
      
      /* Text colors */
      --text-primary: #ffffff;
      --text-secondary: rgba(255, 255, 255, 0.85);
      
      /* Background colors */
      --background: #14101f;
      
      /* Gradients */
      --gradient-primary: linear-gradient(135deg, #9c4dff, #6a36c3);
      --gradient-secondary: linear-gradient(135deg, #00e5ff, #00a3ff);
      --gradient-blend: linear-gradient(135deg, #9c4dff, #00e5ff);
      
      /* Effects */
      --shadow-primary: 0 5px 15px rgba(156, 77, 255, 0.3);
      --shadow-primary-hover: 0 8px 25px rgba(156, 77, 255, 0.4);
      --border-radius: 12px;
      
      /* Animation */
      --transition: 0.3s ease;
      --transition-fast: 0.15s ease;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <h1 class="error-code" data-text="404">404</h1>
    <h2 class="error-title">Page Not Found</h2>
    <p class="error-message">The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.</p>
    
    <div class="error-actions">
      <a href="/" class="error-btn error-btn-primary">
        <i class="fas fa-home"></i>&nbsp;Go to Homepage
      </a>
      <a href="/admin" class="error-btn error-btn-outline">
        <i class="fas fa-tachometer-alt"></i>&nbsp;Dashboard
      </a>
    </div>
    
    <a href="javascript:history.back()" class="error-back-home">
      <i class="fas fa-arrow-left"></i> Go Back
    </a>
  </div>
  
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Set the data-text attribute for the gradient effect
      const errorCode = document.querySelector('.error-code');
      errorCode.setAttribute('data-text', errorCode.textContent);
    });
  </script>
</body>
</html>
