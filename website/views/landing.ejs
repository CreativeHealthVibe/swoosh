<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Swoosh</title>
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
      margin: 0;
      background: #0d0d0d;
      overflow: hidden;
      font-family: 'Segoe UI', sans-serif;
      color: #d6b4fc;
    }

    header {
      position: absolute;
      top: 20px;
      left: 40px;
      z-index: 2;
    }

    header h1 {
      font-size: 2.5rem;
      color: #9c4dff;
      letter-spacing: 2px;
      text-shadow: 0 0 10px rgba(156, 77, 255, 0.5);
      animation: glow 2s ease-in-out infinite alternate;
    }

    @keyframes glow {
      from {
        text-shadow: 0 0 10px rgba(156, 77, 255, 0.5);
      }
      to {
        text-shadow: 0 0 20px rgba(156, 77, 255, 0.8), 0 0 30px rgba(156, 77, 255, 0.6);
      }
    }

    canvas {
      display: block;
    }

    .content {
      position: absolute;
      bottom: 50px;
      left: 40px;
      z-index: 2;
      max-width: 400px;
    }

    .content h2 {
      font-size: 2rem;
      margin-bottom: 10px;
      color: #c792ff;
      background: linear-gradient(90deg, #9c4dff, #d6b4fc);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .content p {
      font-size: 1rem;
      line-height: 1.5;
    }

    .btn {
      display: inline-block;
      margin-top: 20px;
      margin-right: 10px;
      padding: 12px 24px;
      background: #9c4dff;
      border: none;
      border-radius: 8px;
      color: white;
      font-size: 1rem;
      cursor: none;
      transition: all 0.3s;
      text-decoration: none;
      position: relative;
      overflow: hidden;
    }

    .btn:before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
      transition: all 0.6s;
    }

    .btn:hover {
      background: #b168ff;
      transform: translateY(-3px);
      box-shadow: 0 7px 14px rgba(156, 77, 255, 0.3);
    }

    .btn:hover:before {
      left: 100%;
    }
  </style>
</head>
<body class="custom-cursor">
  <!-- Custom cursor elements -->
  <div class="cursor-dot" id="cursor-dot"></div>
  <div class="cursor-outline" id="cursor-outline"></div>
  
  <!-- Particle container -->
  <canvas id="particle-canvas" class="particle-canvas"></canvas>
  
  <header>
    <h1>Swoosh</h1>
  </header>

  <div class="content">
    <h2>Speed. Style. Vibe.</h2>
    <p>Welcome to Swoosh — your new Discord bot for motion, mood, and moderation. Dive into our features and enhance your server experience.</p>
    <a href="https://discord.com/oauth2/authorize?client_id=<%= client && client.user ? client.user.id : '1253391043969974302' %>&permissions=8&scope=bot%20applications.commands" class="btn" target="_blank">Add to Discord</a>
    <a href="/home" class="btn">Explore Features</a>
  </div>

  <!-- Scripts -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
  <script src="/js/landing-background.js"></script>
  <script src="/js/cursor.js"></script>
</body>
</html>
